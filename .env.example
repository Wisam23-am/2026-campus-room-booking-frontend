# API Configuration
REACT_APP_API_BASE_URL=http://localhost:5168
REACT_APP_ENVIRONMENT=development

# Developer Mode (skip login during development)
# Set to 'true' to auto-login without credentials
# Set REACT_APP_DEV_ROLE to 'user' or 'admin' for default role
REACT_APP_DEV_MODE=false
REACT_APP_DEV_ROLE=user